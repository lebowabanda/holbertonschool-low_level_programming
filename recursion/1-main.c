#include "main.h"

/**
 * main - entry point to test _print_rev_recursion function
 *
 * Return: Always 0 (success)
 */
int main(void)
{
    /* Call function to print string in reverse */
    _print_rev_recursion("\nColton Walker");

    return (0);
}